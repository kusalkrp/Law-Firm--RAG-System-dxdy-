# --------------------------------------------------------------------------- #
# Core framework
# --------------------------------------------------------------------------- #
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# --------------------------------------------------------------------------- #
# PDF processing
# --------------------------------------------------------------------------- #
PyMuPDF>=1.24.0,<2.0.0

# --------------------------------------------------------------------------- #
# Vector database (client — server runs in a separate Docker container)
# --------------------------------------------------------------------------- #
chromadb>=0.5.0,<1.0.0

# --------------------------------------------------------------------------- #
# Sparse / keyword retrieval
# --------------------------------------------------------------------------- #
rank-bm25>=0.2.2,<1.0.0

# --------------------------------------------------------------------------- #
# LLM / Embedding / Rerank — all routed through Vercel AI Gateway
# --------------------------------------------------------------------------- #
openai>=1.40.0,<2.0.0
httpx>=0.27.0,<1.0.0

# --------------------------------------------------------------------------- #
# Configuration & validation
# --------------------------------------------------------------------------- #
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# --------------------------------------------------------------------------- #
# Evaluation (offline benchmark only — not a runtime dependency)
# --------------------------------------------------------------------------- #
ragas>=0.2.0,<1.0.0
